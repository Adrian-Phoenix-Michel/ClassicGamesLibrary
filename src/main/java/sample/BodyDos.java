package sample;

import javafx.scene.paint.Color;

import java.util.LinkedList;
/*
public class BodyDos extends SnakeBody {

    BodyDos(SnakeGrid snakeGrid, SnakeCoordinate initialCoordinate) {
        this.color = Color.BLUE;
        this.dead = Color.DARKRED;
        length = 2;
        coordinates = new LinkedList<>();
        coordinates.add(initialCoordinate);
        head = initialCoordinate;
        safe = true;
        this.snakeGrid = snakeGrid;
        xVelocity = 0;
        yVelocity = 0;
    }



    void setUp() {
        if (yVelocity == 2 && length > 1) return;
        xVelocity = 0;
        yVelocity = -2;
    }

    void setDown() {
        if (yVelocity == -2 && length > 1) return;
        xVelocity = 0;
        yVelocity = 2;
    }

    void setLeft() {
        if (xVelocity == 2 && length > 1) return;
        xVelocity = -2;
        yVelocity = 0;
    }

    void setRight() {
        if (xVelocity == -2 && length > 1) return;
        xVelocity = 2;
        yVelocity = 0;
    }


}
*/