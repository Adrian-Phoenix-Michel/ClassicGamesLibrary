package sample;

import javafx.scene.paint.Color;

import java.util.LinkedList;
/*
public class BodyTres extends SnakeBody {

    BodyTres(SnakeGrid snakeGrid, SnakeCoordinate initialCoordinate) {
        this.color = Color.DARKBLUE;
        this.dead = Color.DARKRED;
        length = 3;
        coordinates = new LinkedList<>();
        coordinates.add(initialCoordinate);
        head = initialCoordinate;
        safe = true;
        this.snakeGrid = snakeGrid;
        xVelocity = 0;
        yVelocity = 0;
    }



    void setUp() {
        if (yVelocity == 3 && length > 1) return;
        xVelocity = 0;
        yVelocity = -3;
    }

    void setDown() {
        if (yVelocity == -3 && length > 1) return;
        xVelocity = 0;
        yVelocity = 3;
    }

    void setLeft() {
        if (xVelocity == 3 && length > 1) return;
        xVelocity = -3;
        yVelocity = 0;
    }

    void setRight() {
        if (xVelocity == -3 && length > 1) return;
        xVelocity = 3;
        yVelocity = 0;
    }


}
*/